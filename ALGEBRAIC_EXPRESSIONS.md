# Algebraic expressions

When we evaluate an expression, whether a Lisp expression or an Aperture
expression, the result is another expression.

These expressions may be combined an operated as values, in both the host
language and in the hosted language.

For example, suppose we have expressions A and B.

## Monadic lifting of expressions to values with native data types

