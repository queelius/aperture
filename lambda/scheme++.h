


/**
 * scheme++ is an implementation of scheme whose set of primitive expressions is
 * a generic set of custom, user-defined data types.
 * 
 * this permits efficient computation of domain specific problems, e.g.,
 * matrices
 * 
 * additionally, the abstract syntax tree that results from parsing scheme++
 * code is ...
 * 
 * 
 * 
 *primitive expressions,
 */